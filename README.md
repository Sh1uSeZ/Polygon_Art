# 🧩 OOP Turtle Art Generator

## 🧠 Overview
This program is part of the **OOP Programming Laboratory (Lab 3)** focusing on using **object-oriented programming** with Python’s `turtle` graphics library.  
It generates different **randomized geometric art pieces** depending on the user’s selection (1–9).

Each art piece uses:
- Random polygon sides (triangles, squares, pentagons)
- Random size, orientation, color, and border thickness
- Optional inner/nested polygons for visual variation

---

## 🗂️ Project Structure


---

## ⚙️ How to Run the Program

### **Requirements**
- Python 3.7 or higher
- The `turtle` module (comes preinstalled with Python)
- A display environment (if you’re using macOS/Linux via SSH, ensure GUI forwarding is enabled)

### **Steps to Run**
1. Open a terminal or your IDE.
2. Navigate to the folder containing this program.
3. Run the following command:
   ```bash
   python art_generator.py

